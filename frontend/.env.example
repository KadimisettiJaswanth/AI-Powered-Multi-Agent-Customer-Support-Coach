# The Vite dev server proxies /api -> http://localhost:8000 by default (see vite.config.js).
# For a production build talking to a deployed backend, set this and update
# src/api/client.js's baseURL accordingly (or use an env-driven baseURL).
VITE_API_BASE_URL=http://localhost:8000/api
